export interface HomePageBannerType {
  imageUrl: string,
  targetId: number,
  targetType: number,
  titleColor: string,
  typeTitle: string,
  url: string,
  exclusive: boolean,
  encodeId: string,
}